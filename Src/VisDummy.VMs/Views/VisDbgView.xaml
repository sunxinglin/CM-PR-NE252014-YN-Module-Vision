<UserControl
    x:Class="VisDummy.VMs.Views.VisDbgView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:VMControls.WPF.Release;assembly=VMControls.WPF.Release"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button
                x:Name="load"
                Width="100"
                Margin="5"
                Background="Salmon"
                Click="Load_Click"
                Content="加载方案" />
            <Button
                x:Name="saveAs"
                Width="100"
                Margin="5"
                Background="ForestGreen"
                Click="SaveAs_Click"
                Content="另存为" />
        </StackPanel>

        <vm:VmGlobalToolControl
            Name="vmglobal"
            Grid.Row="1"
            Grid.Column="0"
            Margin="2" />

        <vm:VmMainViewConfigControl
            Name="vmProcCtrl"
            Grid.Row="2"
            Grid.Column="0"
            Margin="2" />
    </Grid>
</UserControl>
