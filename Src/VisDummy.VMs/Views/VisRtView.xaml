<UserControl
    x:Class="VisDummy.VMs.Views.VisRtView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lang="clr-namespace:VisDummy.Lang;assembly=VisDummy.Lang"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="clr-namespace:VisDummy.VMs.Views"
    xmlns:vm="clr-namespace:VMControls.WPF.Release;assembly=VMControls.WPF.Release"
    x:Name="ele"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBox">
                <Setter Property="Width" Value="40" />
                <Setter Property="FontSize" Value="15" />
                <Setter Property="Height" Value="30" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="FontFamily" Value="NSimSun" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="FontFamily" Value="NSimSun" />
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="Margin" Value="2" />
                <Setter Property="FontFamily" Value="NSimSun" />
                <Setter Property="FontSize" Value="15" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="Margin" Value="2" />
                <Setter Property="Padding" Value="10,0" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <vm:VmRenderControl
            Name="vmRenderCtrl"
            Grid.Row="0"
            Grid.Column="1"
            Margin="2" />

        <!--  结果展示  -->
        <Grid
            Grid.Row="1"
            Grid.Column="1"
            Margin="2"
            Background="AliceBlue">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel
                Grid.Column="0"
                Margin="2"
                Orientation="Horizontal">
                <Label x:Name="lblProc" />
                <Button x:Name="btnExecProc" Content="{Binding [触发拍照], Source={x:Static lang:LanguageManager.Instance}}" />
                <Label Content="{Binding [功能号], Source={x:Static lang:LanguageManager.Instance}}" />
                <TextBox x:Name="Function_Number" />
                <Label Content="{Binding [拍照位置], Source={x:Static lang:LanguageManager.Instance}}" />
                <TextBox x:Name="Position_Number" />
                <Label Content="{Binding [批次], Source={x:Static lang:LanguageManager.Instance}}" />
                <TextBox x:Name="Batch" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
